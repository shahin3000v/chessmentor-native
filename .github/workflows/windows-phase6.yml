name: Windows Phase 6 Checkpoint

on:
  push:
    branches: [phase-6]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: windows-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Verify Phase 6 checkpoint
        shell: pwsh
        run: .\verify-and-run-phase6.ps1 -Configuration Release -NoLaunch
